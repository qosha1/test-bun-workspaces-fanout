{
  "name": "test-bun-workspaces-fanout",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "bun --filter '*' build",
    "start": "bun --filter web run start"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
